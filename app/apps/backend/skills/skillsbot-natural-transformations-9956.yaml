name: skillsbot-natural-transformations-9956
display_name: natural-transformations
display_name_zh: 自然变换
description: natural-transformations from SkillsBot scientific academic catalog
description_zh: 本技能专注于范畴论中自然变换的问题解决策略，提供决策树、Lean 4 工具命令和 Yoneda 引理应用，帮助用户验证自然性、分析分量、处理自然同构和函子范畴。
type: analysis
categories:
  - experiment-design
category_zh: 实验设计
subcategory: research-question
subcategory_zh: 研究问题与假设
trigger: manual
auto_recommend: false
tags:
  - 实验设计
  - SkillsBot
url: https://www.skillsbot.cn/skill/9956
source_license: 来源页面未标注，请核对原作者许可
adapted_from: https://www.skillsbot.cn/skill/9956
upstream_name: natural-transformations
upstream_commit: ""
prompt: |-
  ---
  name: natural-transformations
  description: "范畴论中自然变换的问题解决策略"
  allowed-tools: [Bash, Read]
  ---

  # 自然变换

  ## 何时使用

  在范畴论中处理自然变换问题时使用此技能。

  ## 决策树

  1. **验证自然性**
     - eta: F => G 是函子 F, G: C -> D 之间的自然变换
     - 对于 C 中的每个 f: A -> B，图交换：
       G(f) . eta_A = eta_B . F(f)
     - 在 Lean 4 中编写：`theorem nat : η.app B ≫ G.map f = F.map f ≫ η.app A := η.naturality`

  2. **分量分析**
     - 对于每个对象 A，eta_A: F(A) -> G(A)
     - 每个分量是目标范畴 D 中的态射
     - Lean 4: `def η : F ⟶ G where app := fun X => ...`

  3. **自然同构**
     - 每个分量 eta_A 是同构
     - 函子 F 和 G 是自然同构的
     - 记法：F ≅ G (在 Mathlib 中为 NatIso)

  4. **函子范畴**
     - [C, D] 以函子为对象
     - 自然变换为态射
     - 垂直复合：Lean 4 `CategoryTheory.NatTrans.vcomp`
     - 水平复合：`CategoryTheory.NatTrans.hcomp`

  5. **Yoneda 引理应用**
     - Nat(Hom(A, -), F) ~ F(A) 在 A 上自然
     - Lean 4: `CategoryTheory.yonedaEquiv`
     - 完全嵌入 C 到 [C^op, Set]
     - 具体语法参见：`.claude/skills/lean4-nat-trans/SKILL.md`

  ## 工具命令

  ### Lean4_Naturality
  ```bash
  # Lean 4: theorem nat : η.app B ≫ G.map f = F.map f ≫ η.app A := η.naturality
  ```

  ### Lean4_Nat_Trans
  ```bash
  # Lean 4: def η : F ⟶ G where app := fun X => component_X
  ```

  ### Lean4_Yoneda
  ```bash
  # Lean 4: CategoryTheory.yonedaEquiv -- Yoneda 引理
  ```

  ### Lean4_Build
  ```bash
  lake build  # 编译器在环验证
  ```

  ## 认知工具参考

  完整工具文档参见 `.claude/skills/math-mode/SKILL.md`。
