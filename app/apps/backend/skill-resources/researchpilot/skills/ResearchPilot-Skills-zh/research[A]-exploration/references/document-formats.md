# 文档格式规范

`idea_report.md`、`implementation.md`、`dev_log.md` 的详细格式规范。
章节的存在与内容篇幅遵循 `references/template-flexibility.md`。

> **文档分工**：
> - `idea_report.md`：学术内容（文献综述、idea、实验设计），阶段 A/B/C 产出
> - `implementation.md`：编码实现指南（非常详细，直接指导编码），阶段 D 产出，格式见 `references/phase-implementation.md` 第 D-2 节
> - `dev_log.md`：编码进度和日志，阶段 E 维护，格式见 `references/phase-implementation.md` 第 E-1 节
> - `code_guide.md`：**已废弃**，原内容拆分到 `implementation.md`（实现逻辑）和 `code/README.md`（安装/运行）

---

## idea_report.md

> **纯净空白模板**（可直接复制为 `docs/idea_report.md` 开始填写）见 `references/idea_report-template.md`。
> 本节说明格式规范（符号语义、必选/可选章节），空模板只含骨架不含说明文字。

### Markdown 符号语义

| 符号 | 含义 | 约束 |
|------|------|------|
| `#` | 文件总标题 | 全文唯一 |
| `## Part 1` / `## Part 2` / `## Part 3` | 三大部分标题 | 固定，不得增删，文字固定 |
| `### 1` / `### 2` / `### 3` | 一级章节（每部分内从 1 起重新编号） | 固定章节标题见下方模板 |
| `#### 1.1` / `#### 1.2` | 二级小节 | 按内容自由增删，语义化命名 |
| `##### 1.1.1` | 三级小节 | 仅在必要时使用 |
| `>` | 批注 | 紧跟正文之后；对公式、设计决策、来源、难点的解释；**大量使用** |
| `⚠️ [低置信度：...]` | 不确定性标记 | 放在证据不足的论断末尾；在待核实清单中登记 |
| `` `code` `` | 行内代码 | 文件名、函数名、变量名、命令 |
| ` ```text ` | 数据流 / 结构图 | |
| ` ```python ` | 伪代码 | |
| `$$...$$` | 块级公式 | |
| `$...$` | 行内公式 | |
| `**粗体**` | 关键术语首次出现；表格中本文方法行的数值 | 最多 5 词 |
| `1.` 有序列表 | 仅用于参考文献章节 | |
| `---` | Part 之间的分隔；每个审查节点之前 | 仅限这两种位置 |
| HTML 注释 | 最终输出中严格禁止 | |

**标题语言规则（中文插件）**：
- `## Part 1`、`## Part 2`、`## Part 3`、`## References` 保持英文
- 以下一级章节标题保持英文：`Introduction`、`Related Works`、`Method`
- 其余所有标题（二级、三级小节）一律中文

---

### idea_report.md 完整模板

````markdown
# {研究方向} Idea Report
> 生成时间：{YYYY-MM-DD} | 状态：PENDING_REVIEW

---

## Part 1 Topic Overview

### 1 Motivation
{说明这个研究方向的背景：为什么这个问题重要，现有方法存在什么核心局限，研究这个方向的意义是什么。
连续段落，学术风格，引用关键论文。}

> {对上述内容的直白补充：从实际应用角度说明为什么现有方法不够好}

**本研究的必要性：**

- **应用必要性**：{从实际应用场景出发，说明现有方法的局限对下游任务或实际部署有什么具体影响，引用支撑论文}
- **理论必要性**：{指出现有工作留下的认知空白——什么问题至今没有被充分研究或解释，引用支撑论文}
- **时机必要性**：{说明为什么现在是研究这个问题的合适时机，如新数据集的出现、相关技术的成熟、新的应用需求等}

> 注：以上三点必须有文献支撑，不得主观断言。若某点在当前调研中证据不足，标注 ⚠️ [低置信度：待补充证据]。

### 2 Research Questions

{一段引导性陈述，说明这三层 RQ 如何从 Section 1 的核心 gap 推导而来，以及三者之间的递进关系。2–3 句，不超过 100 字。}

> RQ 分三层：RQ1 定义研究目标，RQ2 揭示核心瓶颈（Method 设计依据），RQ3 追问适用边界（可选）。三层对应主实验 → 消融实验 → 附加实验。

#### RQ1：核心问题（研究目标）

**RQ1：{一个大问句，直接描述领域痛点，如"如何提高电池 SOH 预测精度？"}**

- **对应 gap**：{指向 Section 1 中的哪个核心局限，引用支撑论文 [n]}
- **新颖性**：{现有工作是否已完整回答这个问题，本研究与它们的区别}
- **对应实验**：{通过主实验验证，具体验证什么}

#### RQ2：机制问题（方法设计依据）

**RQ2：{一个机制分析问句，分析为什么现有方法做不好，如"局部时序特征是否是 SOH 预测精度的关键瓶颈？"}**

- **与 RQ1 的关系**：{回答 RQ2 如何为解决 RQ1 提供设计依据——它揭示了 RQ1 的核心障碍}
- **对应 gap**：{现有工作是否分析过这个机制，引用支撑论文 [n]}
- **对应实验**：{通过消融实验验证，具体消融什么}

#### RQ3：边界问题（适用范围，可选）

**RQ3：{一个边界/泛化问句，如"提出的模块在不同电池化学体系下是否泛化？"}**（可选，视研究范围增删）

- **与 RQ1/RQ2 的关系**：{追问结论的适用范围，让整体论证更严谨}
- **对应实验**：{通过附加实验验证}

> 注：RQ3 可省略。若省略，在此处注明原因（如"研究范围聚焦，RQ1+RQ2 已充分论证"）。每个 RQ 必须明确、具体、可被实验回答；不得写成泛化的研究方向描述。

### 3 Key Works

{一段引导性说明，描述这批文献的选取逻辑：覆盖了哪些方法类别，为什么这些工作对本研究有参考价值。2–3 句。}

<!-- 汇总表格：仅收录关键工作（5–8 篇）。 -->
| 简称 | 会议/期刊 | 年份 | 核心贡献（一句话） | 对本研究的借鉴价值 |
|------|----------|------|-----------------|----------------|
| {简称} | {Venue} | {年} | {该论文做了什么，15 字内} | {对本研究具体能借鉴什么，15 字内} |

> 表格只列关键工作；下方逐条论文则收录**全部下载的论文**（不止关键工作），简称与标题保持一致。

<!-- 逐条论文：收录全部下载的论文。是否写四点详细介绍由用户在阶段 A 的选择决定（见 phase-research.md A-4.5）。 -->
#### {论文简称}（{Venue} {Year}）[n]
{用户选择需要详细介绍时，按四点展开，学术风格：
1. 解决了什么研究问题；
2. 用了什么方法、为什么这样设计；
3. 这个方法的效果如何；
4. 这篇论文对本研究有什么意义。
用户选择不需要时，仅写一句话核心贡献。}

> 借鉴价值：{对本研究的具体帮助，说明借鉴的是什么——方法设计/实验范式/评估指标/数据处理方式等} [n]
> 是否关键工作：{是 / 否}，因为 {一句话理由——它是否进入了上方的关键工作表格、为什么}

---

## Part 2 Idea Design

### 1 Introduction
{严格按照论文风格写 Introduction。
不允许有小标题。
结构：从大方向（领域重要性）→ 现有方法局限性（逐条，引用论文）→ 本文动机 → 本文方法概述 → 贡献分点列出。
长度：600–1000 字。}

贡献如下：
- 提出了……（方法层面）
- 设计了……（技术层面）
- 在……数据集上验证了……，取得了……的性能提升

> 注：第三条贡献为占位内容，完成实验后用真实数据填写。

### 2 Related Works

#### 2.1 {相关方向一}
{归纳该方向的主要做法，引用代表性论文，指出共性局限。不需要逐篇介绍，重在归纳。}

> {若某个归纳结论直接来自某篇综述，标注来源} [n]

#### 2.2 {相关方向二}
{同上}

#### 2.3 研究空白
{综合以上各方向的不足，明确指出本文要填补的研究空白。
这是 Related Works 的最后一节，固定存在。
格式：先列举现有方法的共性问题，再说明本文从哪个角度切入。}

> {用一句话总结：现有方法"卡在哪里"，本文从哪里突破}

### 3 Method

#### 3.1 {方法整体框架}
{方法的整体思路，先说"做什么"再说"怎么做"。学术风格，3–5 句。}

> {方法的直觉解释：用类比或日常语言说明核心思想}

```text
输入 → [模块A：作用] → [模块B：作用] → [模块C：作用] → 输出
```

> 数据流说明：{对上图每个箭头的文字解释}

#### 3.2 方法流程详解
{用通俗易懂的语言，逐步描述整个方法的每一个流程环节。
不使用公式，不使用学术术语，用"第一步……第二步……"的方式，
让没有该领域背景的读者也能理解方法在做什么。
每个流程步骤与 3.3 中对应的模块小节一一对应，步骤数量与 3.3 小节数量一致。}

> {补充说明：这个流程设计的直觉依据，为什么这样的顺序是合理的}

#### 3.3 {核心模块名称}
{模块详细描述：输入输出定义、操作步骤、公式推导。学术风格。
本节与 3.2 中对应流程步骤一一对应，在理论层面给出严格表述。}

$$
{公式}
$$

> 公式含义：{每个符号的直觉解释；为什么这样设计；这个设计的理论依据}

> 本模块的设计受 [n] 启发，原文中……，本文在此基础上……。[n]
> 原文依据："{PDF 中的支撑句}" (Section {X.X})

#### 3.4 {其他模块……}
{同上，视方法复杂度增减小节，每个小节对应 3.2 中的一个流程步骤}

#### 3.x Baseline 参考与评价指标
{本节固定存在，位于 Method 最后一节。
说明选择哪些 baseline，为什么选这些，以及选择哪些评价指标，为什么。
每个 baseline 和每个指标都必须有论文依据。}

| Baseline | 来源 [n] | 选择理由 |
|---------|---------|---------|
| {名称} | {作者} et al. [n]（{Venue Year}）| {理由} |

> {说明为什么这些 baseline 是公平且有代表性的对比对象}

| 评价指标 | 定义 | 选择依据 [n] |
|---------|------|------------|
| {指标名} | {计算方式} | {引用使用该指标的论文} |

> {说明为什么这些指标能准确反映本方法的创新点}

---

## Part 3 Experiment Design

### 0 Baseline 实验调研

> 本节由阶段 C-1 精读结果直接填入，记录每个 baseline 的实验设计细节，作为后续实验设计的参考基准。

#### 0.x {Baseline 名称}（{Venue} {Year}）[n]

**论文**：{完整标题} | **代码**：{GitHub 链接 或 `[代码不可用]`}

**核心 idea**：{一句话说明这个 baseline 的核心方法，与本研究的关联}

**数据集**：

| 数据集 | 规模 | 划分方式 | 划分比例 / 说明 |
|-------|------|---------|--------------|
| {名称} | {样本数 / 大小} | {随机 / 官方 / 时间顺序 / 交叉验证} | {具体比例或说明} |

**实验设计**：

| 实验 | 目的 | 对比模型 | 评估指标 |
|------|------|---------|---------|
| {主实验名称} | {验证什么} | {列出所有对比模型，逗号分隔} | {指标列表} |
| {消融实验名称} | {验证什么组件} | {消融变体名称} | {指标列表} |

**关键超参数**：batch size = {N}，lr = {float}，epochs = {N}，{其他关键超参数}

> {从代码 / 论文中观察到的值得借鉴或注意的实验设计细节}

---

### 0.x+1 领域惯例归纳

> 本节由阶段 C-2 归纳，综合所有 baseline 精读结果，提炼该领域的实验设计共识，直接指导下方实验设计。

**标准 Benchmark**：{列出各 baseline 共同使用的数据集，说明它们在领域内的地位}

**标准评估指标**：{列出各 baseline 共同使用的指标，说明计算方式和方向（越高 / 越低越好）}

**消融设计惯例**：{说明该领域通常消融哪类组件，常见的消融变体命名方式}

**结果汇报规范**：{是否报告均值 ± 标准差，是否多次随机种子，是否分数据集单独列表}

### 数据与代码可获取性摘要

> 只核实数据集和 baseline 代码能否获取，不涉及 GPU/显存等资源——资源在 Part 3 末尾仅作事后预估。

| 项目 | 状态 | 备注 |
|------|------|------|
| 数据集 {名称} | ✅/⚠️/❌ | {说明} |
| Baseline {名称} 代码 | ✅/⚠️/❌ | {说明} |

---

### 1 数据集

#### 1.1 可用数据集

| 数据集 | 类型 | 规模 | 下载路径 | 用途 |
|-------|------|------|---------|------|
| {名称} | {分类/回归/序列等} | {样本数 / 大小} | {官方链接} | 主实验 / 备用 |

> {说明为什么选这个数据集：它在该领域的标准地位，引用使用它的论文}

#### 1.2 备用数据集

| 数据集 | 类型 | 规模 | 下载路径 | 备用原因 |
|-------|------|------|---------|---------|
| {名称} | {类型} | {规模} | {链接} | {说明} |

#### 1.3 数据预处理
{若该领域有统一的数据预处理项目或工具，必须在此指出，包括项目链接。
若无统一工具，说明标准预处理流程。}

> {说明为什么采用这种预处理方式，有没有论文依据}

### 2 实验设计

**工作量基准（参照顶会/顶刊标准）**：

| 维度 | 最低要求 | 建议目标 |
|------|---------|---------|
| 主实验数据集 | 2 个 | 3–5 个（覆盖不同规模/域） |
| 对比 baseline 数量 | 4 个 | 5–8 个（含近期 SOTA + 经典方法） |
| 消融实验变体 | 每个创新点 1 个 | 3–6 个变体，系统覆盖所有核心设计 |
| 附加实验类型 | 领域标配全做 | 标配之外再补 1–3 类扩展实验 |
| 结果需多次运行 | 是 | 报告均值 ± 标准差，至少 3 次随机种子 |

**实验连贯性要求**：
- 每个实验必须对应 idea 中的至少一个创新点或假设，不得设计与 idea 无关的实验
- 消融实验必须系统覆盖所有核心设计模块，每个模块单独验证
- 附加实验必须验证主实验无法覆盖的性质（如泛化性、效率），不得与主实验重复
- 领域多篇论文重复出现的附加实验视为标配，必做；标配之外再从多角度补足扩展实验供用户选择
- 所有实验使用相同的随机种子集合，确保可复现

> 若某个实验无法满足上述工作量基准，必须在该实验的"实验目的"中说明原因（如：领域公开数据集稀少、计算资源限制等）。

每个实验（主实验、消融实验、附加实验）均使用以下统一格式：

#### {实验序号} {实验名称}

**实验目的**：{这个实验要验证什么——对应 idea 的哪个创新点或假设}

**为什么这样设计**：{说明这个实验为什么采用这样的设置（数据集选择、对比模型范围、指标组合），这样的设计对支撑本文核心方法有什么意义——即这个实验"能证明 idea 的哪一点"。这是本节的核心字段，不得省略。}

**数据集与划分**：

| 数据集 | 训练集 | 验证集 | 测试集 | 划分方式 | 划分理由 |
|-------|-------|-------|-------|---------|---------|
| {名称} | {比例或数量} | {比例或数量} | {比例或数量} | {随机/官方划分/交叉验证} | {理由，引用采用同样划分的论文} |

**评估指标**：

| 指标 | 含义 | 计算方式 | 选择依据 |
|-----|------|---------|---------|
| {指标名} | {用一句话说明这个指标衡量什么} | {公式或计算步骤} | {引用使用该指标的论文} |

> {说明为什么这组指标能准确反映本实验的目的}

**预期效果**：{本方法在这个实验中预期比 baseline 好多少，为什么预期有这样的提升}

**参与评测的模型**：

> 本表逐个说明这个实验选了哪些对比/消融模型、各自与本文方法的区别、纳入它对本 idea 的意义。
> 「出处论文」列仅用于向用户展示信息（该模型由哪篇论文提出、或哪篇论文用它作 baseline），**不参与 baseline 选择决策**。

| 模型 | 与本文方法的区别 | 纳入意义（对 idea 的支撑） | 类型 | 出处论文（仅展示） | 代码链接 |
|-----|---------------|----------------------|------|----------------|---------|
| **Ours** | — | 本文方法 | 本文方法 | — | — |
| {Baseline 1} | {它和我们的方法在哪一点上不同——是缺少哪个创新模块，还是走的另一条技术路线} | {对比它能凸显本文的什么优势 / 证明哪个设计选择是对的} | {经典方法 / 当前 SOTA / 消融变体} | {作者} et al. [n]（{Venue Year}）/ 被 [n] 用作 baseline | {repo 或 N/A} |
| {Baseline 2} | {...} | {...} | {类型} | {出处} | {repo 或 N/A} |

> {说明这一组模型整体的选择逻辑：覆盖了哪些方法类别，为什么这个对比是公平且有代表性的}

**核心 baseline**：{在上表中指明哪一个是核心 baseline（最强或最相关的对比对象），并说明为什么以它为核心——通常是当前 SOTA 或与本文方法最接近、最能凸显创新点的方法。}

**预期结果表（占位）**：

| 模型 | {数据集} | {指标1} | {指标2} | {指标3} |
|-----|---------|--------|--------|--------|
| {Baseline 1} | — | — | — | — |
| {Baseline 2} | — | — | — | — |
| **Ours** | — | **?** | **?** | **?** |

---

以下为各实验的具体内容：

#### 2.1 主实验：综合性能对比
{目的：全面验证本方法在标准 benchmark 上相对于现有方法的综合性能优势。
按上述统一格式填写。}

#### 2.2 消融实验：{核心模块} 有效性验证
{目的：通过逐一去除本方法的各个创新模块，验证每个设计的必要性。
消融变体按以下方式命名：w/o {模块名}，替换为 {基线做法}。
按上述统一格式填写，参与评测的模型为各消融变体——「与本文方法的区别」列说明去掉了哪个模块，「纳入意义」列说明该变体验证哪个设计的必要性。}

#### 2.3 附加实验
{除主实验和消融实验外，每个方法通常还能从其他属性维度进一步验证。本节分两类，均按上述统一格式填写：}

**A. 领域常规附加实验（必做）**
{在阶段 C-1 精读各 baseline 论文时，凡是多篇论文重复出现的附加实验类型（如该领域论文普遍都做的泛化性测试、效率对比），视为本领域的"标配"，本文必须做。逐项列出，并注明"该实验在 [n]、[n] 等论文中均有出现，是本领域验证 {某属性} 的标准做法"。}

**B. 补足工作量的扩展实验（用户选择）**
{在标配实验之外，从尽可能多的角度提出额外实验以补足工作量，供用户挑选。每条注明验证的属性维度和预期能说明的问题。常见角度（不限于）：可解释性（注意力/特征可视化）、泛化性（跨数据集/跨域）、鲁棒性（噪声/对抗/缺失）、效率（FLOPs/参数量/推理延迟）、可扩展性（数据规模/模型规模）、敏感性（超参数）。}

> {对每个附加实验，"为什么这样设计"字段说明它验证什么主实验和消融实验无法覆盖的性质}

### 3 资源预估（参考）

> 本节由阶段 C-6 在实验方案设计完成后填写。**资源预估仅供参考，不是实验设计的约束**——实验设计的第一目的是严格证明 idea 的有效性。

| 实验 | 预估显存 | 预估单次训练时长 | 实验组数 |
|------|---------|---------------|---------|
| 主实验 | ~{N}GB | ~{N} 小时/数据集 | {N} |
| 消融实验 | ~{N}GB | ~{N} 小时/变体 | {N} |
| {附加实验} | ~{N}GB | ~{N} 小时 | {N} |

> 以上为粗略估算，实际资源消耗取决于具体实现与硬件。若资源受限，可在不影响有效性证明的前提下，优先跑核心实验、分批运行。

---

## References

> 格式：MLA。所有条目必须经 web_search 验证为真实存在。无法确认的加 `[待核实]`。
> Part 1 和 Part 2 中的所有引用统一汇总于此，不在各 Part 内部单独列出。

[1] 姓, 名. "论文完整标题." *期刊/会议全称*, vol. 卷, no. 期, 年份, pp. 起始页–终止页.

> **主要工作**：{该论文做了什么，20 字内}
> **引用原因**：{对本 idea 的具体帮助，20 字内}
> **PDF**：`docs/papers/{论文完整标题}.pdf` / `[PDF 不可用]`

---

## 待核实清单
> 由 Claude 自动维护。人工核实后逐一打勾。

- [ ] {论断内容}（位置：{Section}，原因：{PDF 不可用 / 未找到支撑原文 / 数据来自二手引用}）

---

> ⚠️ **阶段 B 完成节点**
> 请审查 Part 1 和 Part 2 后告知 Claude 是否可以进入实验设计阶段。
> - 可以继续 → 直接告知 Claude，进入阶段 C
> - 需要修改 → 提出修改意见，Claude 迭代更新后再次询问确认
````

---

## dev_log.md

```markdown
# 开发日志 — {topic}
> 创建时间：{YYYY-MM-DD} | 最后更新：{YYYY-MM-DD}
> 关联实验设计：`docs/idea_report.md`（Part 3，{更新日期}版本）

## 项目概览
| 项目 | 内容 |
|------|------|
| 研究方向 | {topic} |
| 实现策略 | 从头开始 / 基于 {项目名} |
| 代码根目录 | `code/` |
| 实验设计版本 | {Part 3 更新日期} |
| 语言 | Python |
| 框架 | {PyTorch / TF / JAX} |
| 用户编码要求 | {从对话中收集的要求} |

## 项目架构

```text
code/
├── src/
│   ├── models/
│   │   ├── {model_name}.py         # 本文提出的模型
│   │   └── baseline/
│   │       └── {baseline_name}.py  # 一个 baseline 一个文件
│   ├── data/                       # 多文件时建子目录；单文件直接 src/dataset.py
│   ├── train/                      # 多文件时建子目录；单文件直接 src/train.py
│   ├── evaluate/                   # 多文件时建子目录；单文件直接 src/evaluate.py
│   ├── utils/
│   ├── train.py                    # 训练入口
│   └── evaluate.py                 # 评估入口
├── scripts/                        # nohup shell 脚本，日志 → logs/YY-MM-DD_HH-MM-SS.log
├── configs/                        # 超参数 yaml，每个消融变体一个 config
├── data/                           # 有预处理流程时建 raw/ + processed/；否则直接存放
├── results/                        # gitignored
├── logs/                           # gitignored
├── README.md
└── requirements.txt
```

### 各模块职责
| 文件/目录 | 职责 | 输入 | 输出 | 核心依赖 |
|----------|------|------|------|---------|

## 模型架构（可选）

### 整体结构
```text
输入 → [模块A] → [模块B] → 输出
```

### 核心模块伪代码
```python
class {ModelName}(nn.Module):
    def forward(self, x: torch.Tensor) -> torch.Tensor:
        ...
```

## 项目逻辑

### 数据流
```text
原始文件 → Dataset → DataLoader → transforms → 模型输入
```

### 训练流程
```text
加载 config → 初始化 → epoch 循环 → early stopping → 保存 ckpt
```

### 推理流程
```text
加载 ckpt → 测试集 → forward → 后处理 → 计算指标 → 保存结果
```

## 实现进度

| 模块 | 文件 | 状态 | 完成时间 | 备注 |
|------|------|------|---------|------|
| 初始化 | README.md, requirements.txt, configs/ | ✅ Done | {日期} | |
| 数据加载 | src/data/ | ⬜ TODO | — | |
| 主模型 | src/models/{model}.py | ⬜ TODO | — | |
| Baseline | src/models/baseline/ | ⬜ TODO | — | |
| 训练逻辑 | src/train.py（或 src/train/） | ⬜ TODO | — | |
| 评估逻辑 | src/evaluate.py（或 src/evaluate/） | ⬜ TODO | — | |
| 工具函数 | src/utils/ | ⬜ TODO | — | |
| 运行脚本 | scripts/ | ⬜ TODO | — | |

状态：⬜ TODO / 🔄 WIP / ✅ Done（已运行验证）/ ❌ Blocked

## 开发日志

### {YYYY-MM-DD HH:MM} — {动作简述}
- **完成内容**：{具体内容}
- **遇到的问题**：{问题描述，或"无"}
- **解决方案**：{解决方法，或"无"}

## 已知问题（可选）
- [ ] {问题描述}

---

> 如需回到实验设计阶段修改：告知 Claude 原因，Claude 将归档当前开发日志并回到阶段 C。
```

