# SPE Paper Location

The Paper Agent SPE manuscript package now lives under:

`papers/paper-agent-spe/`

Use that directory as the source of truth for the manuscript, bibliography,
figures, diagram sources, planning notes, and submission readiness checks.
This `docs/spe` directory is intentionally kept only as a pointer so the
repository does not maintain competing paper copies.
