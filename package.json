{
  "name": "coding-kanban-agent-orchestrator",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "dev": "pnpm --filter shared build && concurrently \"pnpm --filter server run dev:app\" \"pnpm --filter web run dev:app\"",
    "dev:restart": "./scripts/restart-dev.sh",
    "build": "pnpm --filter shared build && pnpm --filter server build && pnpm --filter web build",
    "check": "pnpm --filter shared build && pnpm --filter server build && pnpm --filter web build",
    "e2e": "node ./scripts/playwright-e2e-acceptance.mjs",
    "test": "pnpm --recursive test && node --test scripts/*.test.mjs",
    "format": "pnpm --recursive format"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "concurrently": "^9.2.1",
    "dotenv": "^17.4.2",
    "ws": "^8.21.0"
  }
}
